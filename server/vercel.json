{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node@2.0.0"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server.ts" }
  ],
  "env": {
    "NODE_OPTIONS": "--openssl-legacy-provider"
  }

}
